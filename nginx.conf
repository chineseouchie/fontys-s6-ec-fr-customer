server {
  listen 80 default_server;
  server_name localhost test.local;
  location / {
    root /usr/share/nginx/html;
    index index.html index.htm;
    try_files $uri $uri/ /index.html =404;
  }

  location /api/ {
    proxy_pass http://192.168.44.27:30001/:
  }
}
